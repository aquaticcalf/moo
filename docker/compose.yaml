services:
  moo:
    image: ghcr.io/aquaticcalf/moo:latest
    working_dir: /workspace
    volumes:
      - ..:/workspace
      - type: bind
        source: ${MOO_CACHE_DIR}
        target: /root/.moo
    stdin_open: true
    tty: true
